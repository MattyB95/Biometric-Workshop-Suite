services:
  - type: web
    name: biometric-workshop
    runtime: python
    plan: free
    buildCommand: pip install flask gunicorn
    startCommand: gunicorn --bind 0.0.0.0:$PORT src.app:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.14"
